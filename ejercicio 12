using System.Collections.Generic;
using UnityEngine;

public class Ejercicio12 : MonoBehaviour
{
    int[] dañoTotal = new int[4] { 20, 25, 30, 35 };
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int suma = 0;   
        int i = 0;

        while (i < dañoTotal.Length)
        {
            suma += dañoTotal[i];
            Debug.Log("Realizaste " + dañoTotal[i] + " daño total ");
            
            i++;

        }
        Debug.Log("un total de " + suma);

    }

    // Update is called once per frame
    void Update()
