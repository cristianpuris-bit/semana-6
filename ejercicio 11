using UnityEditor. Experimental.GraphView;
using UnityEngine;


public class ejercicioll: MonoBehaviour

{

int[] recompensa = new int[3] { 10, 20, 30 };

void Start()

{
   int a = recompensa[0];
   int b = recompensa [1];
   if (a > b)

    { 
     Debug.Log("el numero mayor es + a);
    }

else if
  (b > a)
   { 
    Debug.Log("el numero mayor es " + b);
   }

else

{
Debug.Log("ambos numeros son iguales");
}

}

// Update is called once per frame

Mensaje de Unity O referencias

void Update()
{

}

} 
