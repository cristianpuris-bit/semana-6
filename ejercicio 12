using System.Collections.Generic;
using UnityEngine;

public class Ejercicio12 : MonoBehaviour
{
    int[] dañoTotal = new int[4] { 20, 25, 30, 35 };
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int a = recompensas[0];
        int b= recompensas [2];

        if (a > b)

      {
       Debug.Log("el numero mayor es " + a);
      }

      else if
        (b > a)
      {
       Debug.Log("el numero mayor es " + b);
      }

      else

      {
       Debug.Log("ambos numeros son iguales");
      }

    }

    // Update is called once per frame
    void Update()
    {

    } 
} 
